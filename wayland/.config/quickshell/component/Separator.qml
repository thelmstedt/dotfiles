import QtQuick
import qs.service

Item {
    implicitWidth: 5
    implicitHeight: parent.height

    Rectangle {
        anchors.centerIn: parent
        width: 0
        height: parent.height
        color: Theme.dim
    }
}
