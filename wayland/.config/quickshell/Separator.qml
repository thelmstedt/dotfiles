import QtQuick
import qs.service

Item {
    implicitWidth: 10
    implicitHeight: parent.height

    Rectangle {
        anchors.centerIn: parent
        width: 2
        height: parent.height
        color: Theme.dim
    }
}
